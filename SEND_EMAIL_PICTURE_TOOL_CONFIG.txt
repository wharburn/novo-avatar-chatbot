=== HUME AI TOOL CONFIGURATION ===

Tool Name: send_email_picture

Description (copy exactly):
Sends a picture via email. Call this tool when the user wants to email a photo. You MUST ask for their full name and email address before calling this tool.

Parameters (JSON Schema):
{
  "type": "object",
  "properties": {
    "email": {
      "type": "string",
      "description": "User's email address"
    },
    "user_name": {
      "type": "string",
      "description": "User's full name"
    },
    "image_url": {
      "type": "string",
      "description": "URL of the image to send (optional - will be auto-filled)"
    },
    "caption": {
      "type": "string",
      "description": "Optional caption for the picture"
    }
  },
  "required": ["email", "user_name"]
}

=== STEPS TO ADD IN HUME AI DASHBOARD ===

1. Go to: https://platform.hume.ai/
2. Click: EVI → Configs → Your Config → Edit
3. Scroll to: "Tools" section
4. Click: "Add Tool" or "Create Custom Tool"
5. Fill in:
   - Name: send_email_picture
   - Description: (paste from above)
   - Parameters: (paste JSON from above)
6. Click: Save
7. Make sure the checkbox next to "send_email_picture" is CHECKED
8. Click: Save Config

=== ALTERNATIVE: USE send_email_summary TOOL ===

If you already have `send_email_summary` tool configured, we can modify the code to use that instead.
Just let me know!

